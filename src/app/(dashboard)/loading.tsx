// Route-level loading UI for the dashboard group (shown during navigation/data
// suspense). Reuses the shared branded loader.
import BrandLoader from '@components/shared/BrandLoader'

const Loading = () => <BrandLoader />

export default Loading
